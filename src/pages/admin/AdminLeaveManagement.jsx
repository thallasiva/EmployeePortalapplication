// This file is intentionally kept as a thin re-export so all existing imports
// (e.g. router config, lazy imports) continue to work without any changes.
// All implementation has been split into focused components under ./adminLeaveManagement/
export { default } from "./adminLeaveManagement/index";
